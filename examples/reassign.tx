function main(): int {
    let x: int = 0;
    println("x = {}", x);

    x = x + 1;
    println("x = {}", x);

    x = x + 1;
    println("x = {}", x);

    x = 42;
    println("x = {}", x);

    return 0;
}